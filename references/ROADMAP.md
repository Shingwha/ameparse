# 路线图

1. **子回路拆分工具**：基于 `subgraph()`/`subcircuits()` 的连通子图划分与
   边界端口导出（图切分是解析器能力的自然延伸；整车图域耦合，需配合
   端口域过滤）。
2. **旧版模型兼容**：pre-2410 的 `.cir` 是行级文本格式，需先跑
   `probes/` 探针校准。
3. **更多成员解析**：`.sad` / `.views` / `.data`（参数值平文件，可做交叉验证）。

背景与决策记录见 [DESIGN.md](DESIGN.md)。
